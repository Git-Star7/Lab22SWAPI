
@{
    ViewData["Title"] = "GetPeopleList";
}
@model PeopleRoot
<h1>Character List</h1>

@for (int i = 0; i < Model.results.Length; i++)
{
    <a class="dropdown-item" href="../StarWars/GetPersonById?id=@i">@Model.results[i].name</a>
}